# Production — Vercel build will use these variables
# Points directly to the Render backend (no proxy in production)
VITE_API_URL=https://zorovex.onrender.com/api

# Razorpay public key (safe to expose — it's a public/client-side key)
VITE_RAZORPAY_KEY_ID=rzp_live_SaSr6bQOkQt8Vu
